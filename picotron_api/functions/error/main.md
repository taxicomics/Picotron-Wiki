# error([description],[unknown])

## Overview

`error` causes a crash with a description (a string) at the top of the stack trace.

## Arguments

### `[description]`: string

The description to show at the top of the stack trace.

### `[unknown]`: unknown

Seen as an argument; what it does is unknown; seemingly useless.