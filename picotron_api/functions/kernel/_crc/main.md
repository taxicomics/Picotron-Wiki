# _crc(string)

## Overview

`_crc` returns the hash of string `string`, where each character corresponds to a single byte

## Arguments

### `string`: string

The string to generate a hash for.

## Returns

### `crc32hash`: integer

The hash of string `string` in bytes, where each character of `string` corresponds to a single byte.