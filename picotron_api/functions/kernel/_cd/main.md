# _cd(kernel_path, [protocol?])

## Overview

`_cd` acts similarly to [cd()](/picotron_api/functions/cd/main.md).

## Arguments

### `kernel_path`: string

The kernel path of the file.

### `[protocol?]`: boolean

Assumed to be whether the path is found at a protocol, rather than locally.