# _fullpath(kernel_path)

## Overview

`_fullpath` returns the full kernel path of the kernel path `kernel_path`.

## Arguments

### `kernel_path`: string

The kernel path of the path to get the full path of.

## Returns

The kernel path of the path `kernel_path`.