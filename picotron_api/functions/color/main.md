# color([col])

## Overview

`color` sets the color of the text cursor to `[col]`.

This is modifying the text cursor, *not* the mouse pointer; for modifying the cursor sprite, see [window()](/picotron_api/functions/window/main.md)

## Arguments

### `[col]`: number

The new color of the cursor, defaults to 6.